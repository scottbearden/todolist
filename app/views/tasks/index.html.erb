<p style="color: green"><%= notice %></p>
<h1>Tasks</h1>
<div id="tasks">
  <div class="row">
    <% @tasks.each do |task| %>
      <%= render task %>
      <p>
        <%= link_to "Show this task", task %>
      </p>
    <% end %>
  </div>
</div>

<%= link_to "New task", new_task_path %>
